# Image Classification using Deep Learning (CNN)

## 📌 Project Overview
This project implements an image classification system using a Convolutional Neural Network (CNN) to classify images as **Cat** or **Dog**.  
The model is built and trained using **TensorFlow and Keras**, and the entire pipeline includes dataset preparation, model training, and prediction.

---

## 🚀 Technologies Used
- Python
- TensorFlow / Keras
- NumPy
- OpenCV
- VS Code
- Git & GitHub

---

## 📂 Project Structure

---

## 🧠 Model Architecture
- Convolution Layer (ReLU)
- Max Pooling
- Convolution Layer (ReLU)
- Max Pooling
- Flatten
- Dense Layer
- Output Layer (Sigmoid)

This CNN learns spatial features from images to classify them accurately.

---

## 📊 Dataset
- Cats vs Dogs image dataset
- Dataset is organized using **directory-based labeling**
- Data is split into **training** and **testing** sets
- Dataset is excluded from GitHub using `.gitignore`

---

## ⚙️ How to Run the Project

### 1️⃣ Clone the repository
```bash
git clone https://github.com/MaheshSwamy7/deep-learning-image-classifier.git
cd deep-learning-image-classifier
